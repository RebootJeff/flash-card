@require 'core'
@require 'flipper'

.profile {
  &.photo {
    border-radius: 0.5rem;
  }

  &.name {
    margin: 2rem;
  }

  &.title {
    margin: 1rem;
  }

  &.description {
    padding: 1rem;
    text-align: left;
  }
}
